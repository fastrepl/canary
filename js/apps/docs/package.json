{
  "name": "@getcanary/docs",
  "scripts": {
    "dev": "vitepress dev",
    "build": "npm run build -w @getcanary/web && vitepress build",
    "preview": "vitepress preview",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "vitepress": "^1.3.1"
  }
}
